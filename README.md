# Vector Ops for Home Assistant

Native Home Assistant integration for the existing Vector Ops update queue. It exposes update inventory and queue state as entities and registers controlled queue services while leaving execution in the existing isolated backend.

## Install

Install through HACS as a custom integration, restart Home Assistant, then add **Vector Ops** from Devices & Services. Use `https://ops.dmhhome.uk` as the backend URL.
